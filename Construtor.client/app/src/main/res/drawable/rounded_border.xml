<!-- res/drawable/rounded_border.xml -->
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/white" /> <!-- Cor de fundo branca -->
    <corners android:radius="20dp" /> <!-- Raio dos cantos arredondados -->
    <stroke android:width="4dp" android:color="@color/Chocolate" /> <!-- Borda laranja -->
</shape>
